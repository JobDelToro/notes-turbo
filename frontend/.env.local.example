# Copy to `.env.local` for local development.
# Browser-side base URL for the API. Cookies are httpOnly, so every request is
# made with `credentials: 'include'` (see lib/api.ts).
NEXT_PUBLIC_API_URL=http://localhost:8000/api
